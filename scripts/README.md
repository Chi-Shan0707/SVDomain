# Scripts

This directory stays intentionally small.

## Included script

- `run_rl_checkpoint_ranking_linear_heads.py`

## Root-level wrappers

The following public entry wrappers are kept at the repo root:

- `run_lowrank_necessity_ablation.py`
- `run_cross_anchor_transfer.py`
- `run_dense_anchor_earlystop.py`
- `run_dense_cross_anchor_transfer.py`

## Why so few

The cleaned repo is not trying to vendor the full research workspace. It only keeps the scripts that still help explain the retained artifacts.


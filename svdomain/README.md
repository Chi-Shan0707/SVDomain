# `svdomain`

Only the smallest local helper code is kept here.

## Included file

- `bootstrap_nad_next.py`

Its job is simple: find the sibling `NAD_Next` workspace and put it on `sys.path` when the retained entry wrappers need it.


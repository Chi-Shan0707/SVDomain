# Low-rank Necessity Scan

这两份 JSON 是 `07_LOWRANK_NECESSITY.md` 的机器可读补充：

- `lowrank_necessity_summary.json`
- `lowrank_necessity_eval.json`

它们被保留，是因为：

- 体积很小；
- 能补足 `csv + markdown` 之外的可追溯性；
- 不会把仓库重新拖回“大而杂”的状态。

